<div id="content-title">
<h2>Record some new experience.</h2>
</div>
<p>Enter details of your experience below - probably work-based, but we know other things count too...<br />
Making the experience <strong>public</strong> means <strong>anyone</strong> who visits your portfolio <strong>will be able to see it</strong>.</p>
<%= error_messages_for :experience %>

<% form_for :experience, :url => { :action => 'create' } do |f| %>
	<div>
		<label>What was your role?</label><br />
		<%= f.text_field :role, :class => 'medium' %>
	</div>

	<div>
		<label>Who was your employer?</label><br />
		<%= f.text_field :employer, :class => 'medium' %>
	</div>

	<div>
		<label>Where was it based?</label><br />
		<%= f.text_field :location, :class => 'medium' %>
	</div>

	<div>
		<label>When did you start?</label><br />
		<%= f.date_select(:start_date, :discard_day => true, :order => [:month, :year], :start_year => 1920, :end_year => DateTime.now.year) %>
	</div>

	<div>
		<label>When did you finish?</label><br />
		<%= f.date_select(:end_date, :discard_day => true, :order => [:month, :year], :start_year => 1920, :end_year => DateTime.now.year) %>
	</div>

  <div>
    <label>What did you gain?</label><br />
    <%= f.text_area :notes, :cols => 69 %>
  </div>

	<div>
		<label>Privacy setting: </label>
		<%= f.select(:privacy, [ ['Private', 'private'], ['Shared', 'shared'], ['Public', 'public'] ]) %>
	</div>

	<div>
		<%= f.submit "Create" %> or <%= link_to "go back to your experiences", :action => 'index' %>
	</div>
<% end %>
