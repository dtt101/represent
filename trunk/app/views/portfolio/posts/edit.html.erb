<div id="content-title">
<h2>Edit a Blog Post</h2>
</div>
<p>Complete this form to edit a blog post.<br />Making the post <strong>public</strong> means <strong>anyone</strong> who visits your portfolio <strong>will be able to see it</strong>.</p>
<%= error_messages_for :post %>

<% form_for :post, :url => { :action => 'update', :post_id => @post.id } do |f| %>
  <div>
    <label>Edit the title</label><br />
    <%= f.text_field :title, :class => 'large' %>
  </div>

  <div>
    <label>Update your blog post</label><br />
    <%= f.text_area :body, :cols => 69 %>
  </div>

  <div>
    <label>Privacy setting: </label>
	<%= f.select(:privacy, [ ['Private', 'private'], ['Shared', 'shared'], ['Public', 'public'] ]) %>
  </div>
  <div>
    <%= f.submit "Save" %> or <%= link_to "go back to your blog", :action => 'index' %>
  </div>
<% end %>
